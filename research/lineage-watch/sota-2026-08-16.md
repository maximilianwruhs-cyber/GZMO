# Lineage SOTA — 2026-08-16

**Host:** telescope. Living vault not re-attested in this file.  
**Supersedes as *watch list*:** [sota-2026-08-15.md](sota-2026-08-15.md) (keep that file).  
**Atlas (three planes):** [../agentic-memory-harness-local-llm-2026-08-16.md](../agentic-memory-harness-local-llm-2026-08-16.md)  
**Active ship unchanged:** `felt-use-mass-growth`.

This is a **delta**, not a second manifesto. 2026-08-15 covered memory fields A–F. Today adds planes **G (harness)** and **H (frozen-model lift)** and corrects the graft queue against `gzmo-core` after #166/#167.

---

## Correction — grafts 1, 2, 4 are in-tree

Do not re-queue as new work:

| Was “next graft” | Now |
|------------------|-----|
| Outcome-linked utility | `FeltUseKind::Outcome` + `reinforce_outcome_from_new_truths` |
| Dream as region rewrite | `SqliteVault::maybe_region_rewrite` + `gate_event = 'region_rewrite'` |
| Verify rollback + typed supersession | `gate_event`, quarantine, `failure_cases` insert + bounded retrieve |
| Named night labels | Docs/scheduler only |

Still open from that list: **named night labels** (docs/scheduler). New open: **pin** workflow skill text in `context.rs`.

---

## Field G — Harness / context engineering (new watch)

| Work | Steal | Non-steal |
|------|-------|-----------|
| HumanLayer harness engineering | Pin skill instructions against compaction; subagent isolation; MCP descriptions are prompt | Cursor/Claude as the GZMO product |
| Agent Skills (`SKILL.md`) | Progressive disclosure (index cheap, body on trigger) | Marketplace mass-install |
| SWE-agent / OpenHands | Tests as back-pressure | Cloud sandbox SKU |

**GZMO delta:** `agent_loop.rs`, `context.rs` prune+archive, `workflow_skills`, `subagent.rs`, `gzmo mcp-serve` already exist. Next steal is **H1** (pin skill bodies), not a new runtime.

---

## Field H — Frozen-model lift without weights (new watch)

| Work | Steal | Non-steal |
|------|-------|-----------|
| **ACE** ([2510.04618](https://arxiv.org/abs/2510.04618), ICLR 2026) | Incremental playbook `ADD/UPDATE/REMOVE`; Reflector ≠ Curator; execution feedback | Overnight rewrite of living SOUL/toml; extra model gym |
| DSPy / GEPA / MIPRO | Eval-then-delta | Python optimizer crate in `gzmo-core` |
| Arena / calibration (ours) | Human pin | Auto engine swap |

**GZMO delta:** we already refuse LoRA-sleep. ACE is the honest name for “skills and SOUL evolve under a gate.” Observatory summaries are the anti-pattern (brevity bias).

---

## Field A–F (memory) — one-line delta

- **A-Mem** (NeurIPS 2025, 2502.12110): neighbor evolve on write. We keep **verify-then-promote** (spark). Do not LLM-rewrite honeypot neighbors unguarded.
- **Agent Memory characterization** (2606.06448): write-path mutation is expensive. Overnight batch (daemon) is the right systems answer — we already chose it.
- **MemPalace** leaderboard noise: borrow-eval only. Not soak GREEN.

---

## Watch items (candidate grafts — not active)

0. Living felt-use mass + soak nights 2–3 (active bet).  
1. Pin workflow `SKILL.md` against context prune.  
2. Named night TRIAGE → CONSOLIDATE → AUDIT (labels).  
3. ACE curator deltas on **one** workflow skill, human-pinned.

---

## Next refresh

On or before **2026-09-16**, or sooner if ACE/harness work lands a measurable steal. Link from [README.md](README.md).
