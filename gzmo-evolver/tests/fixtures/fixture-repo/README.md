# Fixture repository for Stage 1 hermetic acceptance

Deterministic seed tree for `fixture_run_reaches_evaluating_without_remote_mutation`.
Contains only committed fixture data — no network, credentials, or ambient scripts.
