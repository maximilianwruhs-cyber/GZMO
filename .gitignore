# ── Rust ──────────────────────────────────────────────────────────────────
target/

# ── Secrets & local config ───────────────────────────────────────────────
.env
.env.local
.env.*
!.env.template
*.pem
*.key
.mcp.json

# Local operator config (copy from gzmo.toml.example)
gzmo.toml
config/shared-mcp-memory.json

# Accidental home / IDE dotfiles (never commit)
.bash_profile
.bashrc
.zshrc
.zprofile
.profile
.gitconfig
.claude/
.idea/
.vscode/
.ripgreprc

# ── Runtime / agent state (never commit) ───────────────────────────────────
/memory/
!memory/.gitkeep
data/vault.db
data/vault.db-*
data/vault.db.pre-*
# M5 derived export (regenerated by scripts/ripen-knowledge-core.py)
data/knowledge_core.db
data/knowledge_core.db-*
data/knowledge_core.candidates.json
data/knowledge_core_export.md
data/sessions/
data/backups/
data/distill-queue/
data/Synapse/
logs/
reports/
DREAMS.md
CHAOS_STATE.json
data/HEARTBEAT.md
data/pi-frontend-session.id
data/synapse-reader.state.json
data/kurator-monitor.state.json
data/synapse-pi-distill.state.json
data/synapse-pi-display.state.json
data/synapse-writer-gate.state.json
data/dice_loop_state.json
data/chaos_feedback_audit.jsonl
data/honeypot-confidence-report.json
data/learner/
data/skills/
data/pedagogy/edf_log.jsonl
data/pedagogy/low_tension_dialogue.jsonl
data/pi-mentor-discovery-cycle-*.md
data/cycle-*.md
data/arxiv-cache/
cycle-*.md
chaos_card_output.log

# Keep static seed data
!data/lore.toml
!data/.gitkeep

# ── Models & binaries (large / machine-specific) ───────────────────────────
models/
bin/
bin/*.so*
bin/llama-*

# ── Agent IDE / tooling (local only) ───────────────────────────────────────
.pi/
.Jules/

# ── Python / eval artifacts ────────────────────────────────────────────────
__pycache__/
*.pyc
scripts/ingest-quality/reports/
scripts/ingest-quality/.batch2-corpus/
scripts/ingest-quality/report.json
scripts/ingest-quality/report-batch2.json
scripts/ingest-quality/*.log
scripts/ingest-quality/*.stderr.log
scripts/.venv/
.venv/
experiments/toto-spike/.venv/
experiments/toto-spike/results/*.pt
experiments/toto-spike/.hf_cache/

# ── Session research docs (local working notes) ────────────────────────────
docs/archive/

# ── Wiki layer (git-tracked on purpose — Obsidian + git review) ─────────────
# Unlike memory/ and data/vault.db, wiki/ markdown IS committed. Only locally
# downloaded source images are ignored.
wiki/assets/
!wiki/.gitkeep
!wiki/**/.gitkeep

# ── OS / editor ────────────────────────────────────────────────────────────
.DS_Store
*.swp
*.swo
*~

/.crew/
/.crew
!.crew/artifacts/
!.crew/graphs/
!.crew/artifacts/.gitkeep
!.crew/graphs/.gitkeep

# GZMO skills Chimera dir
/gzmo_skills/

# SIL local index + recall smoke artifacts
.gzmo/
data/discovery-kb-metrics/recall-smoke-*.json
data/eval/
gzmo-core/data/

# IDE-local rules
.cursor/